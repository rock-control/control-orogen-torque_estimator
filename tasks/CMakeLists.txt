# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(torque_estimatorTaskLib)
ADD_LIBRARY(${TORQUE_ESTIMATOR_TASKLIB_NAME} SHARED 
    ${TORQUE_ESTIMATOR_TASKLIB_SOURCES})

add_dependencies(${TORQUE_ESTIMATOR_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${TORQUE_ESTIMATOR_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${TORQUE_ESTIMATOR_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${TORQUE_ESTIMATOR_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${TORQUE_ESTIMATOR_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${TORQUE_ESTIMATOR_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${TORQUE_ESTIMATOR_TASKLIB_HEADERS}
    DESTINATION include/orocos/torque_estimator)

